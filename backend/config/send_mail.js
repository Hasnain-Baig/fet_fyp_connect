const {MY_MAIL,MY_MAIL_APP_PASSWORD} =require('./var');
const jwt=require('jsonwebtoken');
const nodemailer=require('nodemailer');

const sendMail=async (to,subject,link,successMsg)=>{



}

module.exports={sendMail};